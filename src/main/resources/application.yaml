server:
  port: 8086

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/netscape_user_db
    username: postgres
    password: Benklins@123
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect